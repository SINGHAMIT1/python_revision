# tuples is a list of values group together
# in list all values have same meaning
# though in tuples all values have different meaning,
# tuples are heterogeneous, list are homogenous
# list examples: expenses list,list of names
# tuples example: coordinates, address
point = (1, -2)
print(point)
print(point[0])
# tuples are immutable, nut lists are mutable
point[0] = 5
print(point)
